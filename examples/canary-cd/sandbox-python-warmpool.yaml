apiVersion: extensions.agents.x-k8s.io/v1alpha1
kind: SandboxWarmPool
metadata:
  name: python-pool
  namespace: default
spec:
  sandboxTemplateRef:
    name: sandbox-python-template-v1
  replicas: 5
